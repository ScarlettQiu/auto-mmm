numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.10.0
tabulate>=0.9.0
python-pptx>=0.6.21

# Optional — Bayesian MMM (recommended)
# pip install pymc-marketing

# Optional — Google LightweightMMM (requires JAX)
# pip install lightweight_mmm
# pip install jax jaxlib  # Mac: pip install jax[cpu]
